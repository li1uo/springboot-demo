<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <groupId>demo.springboot</groupId>
        <artifactId>starter-demo</artifactId>
        <version>0.0.1.RELEASE</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>springboot-starter-auto</artifactId>
    <name>${project.artifactId}</name>
    <version>${springboot.demo.version}</version>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>demo.springboot</groupId>
            <artifactId>springboot-common</artifactId>
            <version>${springboot.demo.version}</version>
        </dependency>

        <dependency>
            <groupId>demo.springboot</groupId>
            <artifactId>springboot-starter-mybatis</artifactId>
            <version>${springboot.demo.version}</version>
        </dependency>

        <dependency>
            <groupId>demo.springboot</groupId>
            <artifactId>springboot-starter-boot</artifactId>
            <version>${springboot.demo.version}</version>
        </dependency>

        <dependency>
            <groupId>demo.springboot</groupId>
            <artifactId>springboot-starter-db</artifactId>
            <version>${springboot.demo.version}</version>
        </dependency>

        <dependency>
            <groupId>demo.springboot</groupId>
            <artifactId>springboot-starter-redis</artifactId>
            <version>${springboot.demo.version}</version>
        </dependency>

        <dependency>
            <groupId>demo.springboot</groupId>
            <artifactId>springboot-starter-tool</artifactId>
            <version>${springboot.demo.version}</version>
        </dependency>
    </dependencies>
</project>
