<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="demo.springboot.core.mapper.RoleMapper">

     <resultMap id="baseResultMap" type="demo.springboot.core.domain.vo.RoleVO">
          <id property="roleId" column="role_id"/>
          <result property="roleName" column="role_name"/>
          <result property="roleCode" column="role_code"/>
     </resultMap>

     <sql id="baseSql">
          role_id,role_name,role_code
     </sql>

     <!-- 根据用户名查找角色信息 -->
     <select id="listRoleByUserName" parameterType="string" resultType="string">
          select distinct r.role_code
          from t_user u
          inner join t_user_role ur
          on u.user_id = ur.user_id
          inner join t_role r
          on ur.role_id = r.role_id
          where u.user_name = #{userName}
     </select>
</mapper>